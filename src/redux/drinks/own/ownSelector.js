export const selectOwnItems = (state) => state.own.items;

export const selectIsLoading = (state) => state.own.isLoading;

export const selectAdded = (state) => state.own.added;

export const selectError = (state) => state.own.error;
